//_ Element Silme
const todoList = document.querySelector(".list-group");
const todos = document.querySelectorAll(".list-group-item");
const todo1 = document.querySelector(".list-group-item");



// todos[0].remove(); // 0. indexteki elementi siler.
// todoList.removeChild(todos[1]); // 2. indexteki elementi siler.

// todos[todos.length-1].remove(); //son child'ı siler.

// todo1.remove(); //ilk child'ı siler.

// todoList.removeChild(todos[0]); //todolist içerisindeki 0'nci indeks deki elementi siler.

// todoList.removeChild(todoList.lastElementChild); //4. elementi siler

console.log(todos); // Silinen elementleri gösterir.





